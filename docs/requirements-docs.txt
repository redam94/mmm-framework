# Sphinx toolchain for the Read the Docs build (.readthedocs.yaml).
# Keep in lockstep with [dependency-groups] docs in pyproject.toml — RTD's
# python.install cannot install PEP 735 dependency groups, so this file is the
# RTD-side mirror.
#
# The package itself is installed separately by RTD (`pip install .` — the
# LEAN core since the 1.0 packaging split), so runtime deps do NOT belong
# here; a stale numpy<2 pin used to live in this file and would have made the
# resolver fail against the core's numpy>=2.3.5. conf.py additionally mocks
# the heavy numerical stack (pymc/jax/numba/...) during autodoc.

sphinx>=7.0,<8.0
furo>=2024.8.6
sphinx-autodoc-typehints>=2.0
sphinx-copybutton>=0.5
myst-parser>=3.0
sphinxcontrib-mermaid>=0.9
