# 4c — the gVisor sandbox runtime (adopted only if the trust model escalates, §7.2).
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata: { name: gvisor }
handler: runsc        # gVisor; use `kata` for Kata Containers
